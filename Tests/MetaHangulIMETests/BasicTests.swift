import XCTest
@testable import MetaHangulIME

final class BasicTests: XCTestCase {
    func testExample() throws {
        // This is a basic test to ensure the module builds
        XCTAssertEqual(1 + 1, 2)
    }
}